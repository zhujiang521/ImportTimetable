<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="com.zui.calendar.permission.ACCESS_CALENDAR" />
    <uses-permission android:name="com.zui.calendar.permission.READ_CURRICULUM" />
    <uses-permission android:name="com.zui.calendar.permission.WRITE_CURRICULUM" />

    <queries>
        <package android:name="com.zui.app.calendar" />
        <provider android:authorities="com.zui.app.calendar" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImportTimetable">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ImportTimetable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ApiTestActivity"
            android:exported="false"
            android:label="API测试工具"
            android:theme="@style/Theme.ImportTimetable" />

    </application>

</manifest>